// Create a function, getFirst(arr), that returns the first item in the array
function getFirst(arr)
{
  return arr[0];
}


// Call it with an array as an argument to verify that it works
let firstCard = getFirst(["one", "two", "three", 4, 6, true, false, "hoja"]);

console.log(firstCard);

















/* // What are greeting and name? parameter
// What are "Howdy" and "James"? arguments
// What are num1 and num2? parameter
// What are 3 and 4? argument



let welcomeEl = document.getElementById("welcome-el")

//.                   parameters
function greetingUser(greeting, name) {
  welcomeEl.textContent = `${greeting}, ${name} 👋`;
}


let hi = "Howdy"
//.           arguments
greetingUser(hi, "James");

function add(num1, num2)
{
  return num1+num2;
}


console.log( add(3,4)    ) // should log 7
console.log( add(9, 102) ) // should log 111

 */


















/* // Create a function, add(), that adds two numbers together and returns the sum

function add(num1, num2)
{
  return num1+num2;
}


console.log( add(3,4)    ) // should log 7
console.log( add(9, 102) ) // should log 111

 */















/* const welcomeEl = document.getElementById("welcome-el");

// Give the function a parameter, greeting, that replaces "Welcome back"
function greetUser(greeting, user, emoji){
  welcomeEl.textContent = `${greeting}, ${user} ${emoji}`
}

greetUser("Hello", "Akash Shaykh", "🔥"); */










/* const welcomeEl = document.getElementById("welcome-el");

function greetUser(user){
  welcomeEl.textContent = `Welcome back, ${user} 👋`
}

greetUser("Akash"); */